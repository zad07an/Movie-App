"use client";

import React from "react";
import { HashLoader } from "react-spinners";

const Loader: React.FC = (): JSX.Element => {
  return <HashLoader color="#7E22CE" />;
};

export default Loader;
